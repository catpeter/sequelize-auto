// 名字设置为数据库名字，便于输入 pyw
const liang = {
  host: 'your host',
  database: 'your database',
  user: 'your username',
  pass: 'your password',
  port: 'your post',
  require: 'your require' // var sequelize = require('../config/settings'); settings 就是require
}

const test_lyg = {
  host: 'your host',
  database: 'your database',
  user: 'your username',
  pass: 'your password',
  port: 'your post',
 require: 'your require'
}

module.exports = {
  liang,
  test_lyg
}